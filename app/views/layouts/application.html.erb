<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Proplatform</title>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Estilos CSS -->
  <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
  <%= javascript_importmap_tags %>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

  <!-- Estilos adicionales -->
  <style>
    body {
      background-color: #f8f9fa; /* Light grey background */
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      margin: 0;
      font-family: 'Arial', sans-serif;
    }
    .welcome-title {
      color: #343a40; /* Dark grey text */
      font-size: 3rem;
      font-weight: bold;
      text-align: center;
      background: linear-gradient(135deg, #dd1728, #6f42c1, #d63384);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      margin-top: 20vh; /* Adjust vertical margin to center title */
    }
    .container {
      flex: 1;
    }
    /* Estilos para la navbar */
    .navbar {
      background-color: #343a40; /* Dark grey background */
    }
    .navbar-brand {
      color: #ffffff !important; /* White text for brand */
      font-weight: bold;
    }
    .navbar-nav .nav-link {
      color: #ffffff !important; /* White text for links */
    }
    .navbar-nav .nav-link:hover {
      color: #d63384 !important; /* Pink color on hover */
      background-color: rgba(255, 255, 255, 0.1); /* Slightly transparent background */
      border-radius: 4px;
    }
    .dropdown-menu {
      background-color: #343a40; /* Dark grey background for dropdown */
    }
    .dropdown-item {
      color: #ffffff !important; /* White text for dropdown items */
    }
    .dropdown-item:hover {
      background-color: #d63384 !important; /* Pink background on hover */
      color: #ffffff !important; /* White text on hover */
    }
    /* Estilos para mensajes flash */
    .alert {
      background-color: transparent; /* Remove background color */
      border: none; /* Remove border */
      color: #343a40; /* Dark grey text color */
      box-shadow: none; /* Remove box shadow */
      padding: 10px 20px; /* Add padding */
    }
    .alert-info {
      color: #007bff; /* Blue color for info messages */
    }
    .alert-danger {
      color: #dc3545; /* Red color for danger messages */
    }
  </style>
</head>

<body>
  <!-- Navbar -->
  <header>
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">MyApp</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if signed_in? %>
              <li class="nav-item">
                <%= link_to "Profile", edit_user_registration_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Login", new_user_session_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
  </header>

  <!-- Main Content -->
  <main class="container">
    <h1 class="welcome-title">Bienvenido a pro_platform</h1>

    <!-- Flash messages -->
    <!--div class="alert alert-info" role="alert">
      <%= notice %>
    </div>
    <div class="alert alert-danger" role="alert">
      <%= alert %>
    </div-->

    <%= yield %> <!-- Renderiza el contenido de la vista específica -->
  </main>

  <!-- Bootstrap JS (optional, for interactive components) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
