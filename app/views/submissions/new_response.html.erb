<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Responder Formulario</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Responder Formulario: <%= @form.title %></h1>

    <!-- Formulario para responder preguntas -->
    <%= form_with(model: [@form, @submission], local: true) do |form| %>
      <% JSON.parse(@form.fields).each do |field, type| %>
        <div class="mb-3">
          <%= form.label field.capitalize %>
          <%= form.text_field "responses[#{field}]", class: "form-control", required: true %>
        </div>
      <% end %>

      <div class="d-flex justify-content-center mb-3">
        <%= form.submit "Enviar Respuestas", class: "btn btn-primary me-2" %>
        <%= link_to "Cancelar", forms_path, class: "btn btn-outline-danger me-2" %>
      </div>
    <% end %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
