<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gestión de Formularios - Proplatform</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Gestión de Formularios</h1>

    <!-- Botón para crear un nuevo formulario -->
    <div class="text-center mb-4">
      <%= link_to "Crear Nuevo Formulario", new_form_path, class: "btn btn-primary" %>
    </div>

    <!-- Lista de formularios existentes -->
    <div class="row">
      <% @forms.each do |form| %>
        <div class="col-md-4 mb-3">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"><%= form.title %></h5>
              <p class="card-text"><%= truncate(form.description, length: 100) %></p>
              
              <!-- Información del creador y fecha -->
              <p class="card-text text-muted">
                Creado por: <%= form.user.full_name %> <br>
                Fecha: <%= form.created_at.strftime("%d/%m/%Y") %>
              </p>
              
              <div class="d-grid">
                <%= link_to "Ver Formulario", form_path(form), class: "btn btn-outline-secondary mb-2" %>
                <%= link_to "Editar", edit_form_path(form), class: "btn btn-outline-primary mb-2" %>
                <%= button_to "Eliminar", form_path(form), method: :delete, data: { confirm: "¿Estás seguro de que quieres eliminar este formulario?" }, class: "btn btn-outline-danger mb-2 w-100" %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Bootstrap JS (opcional, para componentes interactivos) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
